import { SuccessContent } from './SuccessContent'
import type { ContentRevealKey } from '../animation/animation-timeline'

/** Re-export for convenience; SuccessContent owns the Step 7 layout. */
export { SuccessContent }
export type { ContentRevealKey }
